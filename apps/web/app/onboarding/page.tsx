import { OnboardingClient } from "@/components/OnboardingClient";

export const metadata = {
  title: "Choose Your Role — AURA Terminal",
};

export default function OnboardingPage() {
  return <OnboardingClient />;
}
