import { AuthPanel } from "@/components/AuthPanel";

export const metadata = {
  title: "Sign Up — AURA Terminal",
};

export default function SignupPage() {
  return <AuthPanel variant="signup" />;
}
