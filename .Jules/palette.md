## 2026-08-21 - Adding accessibility markup safely to React elements
**Learning:** When generating dynamic ID links like `htmlFor` in reusable components, utilizing `useId()` instead of static IDs guarantees robust accessibility without clashes.
**Action:** Use `useId()` in React inputs and labels consistently instead of generic strings.
